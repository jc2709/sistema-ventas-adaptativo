import 'package:flutter/material.dart';

class EmotionDebugScreen extends StatelessWidget {
  const EmotionDebugScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Prueba de emociones: pendiente de integrar modelo TFLite'),
      ),
    );
  }
}
